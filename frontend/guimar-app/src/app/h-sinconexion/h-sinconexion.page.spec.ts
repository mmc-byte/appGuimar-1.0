import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HSinconexionPage } from './h-sinconexion.page';

describe('HSinconexionPage', () => {
  let component: HSinconexionPage;
  let fixture: ComponentFixture<HSinconexionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HSinconexionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
